sirocco:
  compiler: [gcc]
  specs:
    - python@3.14
  unify: true
  views:
    app:
      uenv:
        add_compilers: true
        env_vars:
          set:
            - VIRTUAL_ENV: "${@mount@}/sirocco_venv"
          prepend_path:
            - PATH: "${@mount@}/sirocco_venv/bin"
